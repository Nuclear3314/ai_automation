# 基础配置
CONFIG = {
    "logging": {
        "level": "INFO",
        "format": "%(asctime)s [%(levelname)s] %(message)s"
    },
    "data_collection": {
        "cache_duration": 3600,  # 1小时
        "retry_attempts": 3
    }
}